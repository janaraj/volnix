"""Internal helpers shared across the volnix platform.

Modules here are implementation details — they are NOT part of the
public API and may change between minor releases. Public surfaces
must never re-export from ``volnix._internal``.
"""
