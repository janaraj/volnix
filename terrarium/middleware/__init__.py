"""API surface middleware for Terrarium.

Optional middleware layers that make Terrarium behave like real service
APIs: auth token validation, HTTP status code mapping, and service URL
prefix routing. All configurable via terrarium.toml.
"""
